<h1><%= t ('brewery.admin.auth_core.users.title') %></h1>

<noscript>
	<%= will_paginate @users %>
</noscript>
<table class="table table-striped">
	<thead>
		<tr>
			<th>EMAIL</th>
			<th>EMAIL</th>
			<th><%= t 'brewery.general.actions_title' %></th>
		</tr>
	</thead>

	<tbody id="infinite-scroll" data-infinite-url="/admin/users" data-infinite-current-page="1">
		<%= render 'users', users: @users %>

		<tr class="loading hide">
			<td colspan="3">
				<%= css_loading_icon %>
				<%= t 'brewery.general.loading_more_results' %>
			</td>
		</tr>
	</tbody>
</table>

<noscript>
	<%= will_paginate @users %>
</noscript>