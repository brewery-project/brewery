$('<%= escape_javascript(render 'users', users: @users) -%>').insertBefore($("#infinite-scroll .loading")).fadeIn()

<% if !@users.next_page || @users.out_of_bounds? %>
	$("#infinite-scroll").data('infinite-last-page-reached', 'true');
<% end %>